import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  allowedDevOrigins: ["192.168.0.207"], // Replace with your exact computer IP
};

export default nextConfig;
